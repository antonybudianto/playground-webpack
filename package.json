{
  "name": "playground-webpack",
  "version": "1.0.0",
  "private": "true",
  "description": "playground webpack",
  "main": "index.js",
  "scripts": {
    "dev": "webpack --mode=development --watch",
    "build": "webpack --mode=production",
    "build-run": "npm run build && node ./dist/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "demo",
  "license": "MIT",
  "dependencies": {
    "dayjs": "^1.11.2"
  },
  "devDependencies": {
    "webpack": "^5.72.1",
    "webpack-cli": "^4.9.2"
  }
}
